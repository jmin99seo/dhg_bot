apiVersion: v1
kind: Service
metadata:
  name: {{ include "dhg_bot.fullname" . }}
  labels:
    {{- include "dhg_bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dhg_bot.selectorLabels" . | nindent 4 }}
